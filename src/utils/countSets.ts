// src/utils/countSets.ts
// Counts how many sets exist in a list.
// This mirrors iSkiLog logic where a "set" is one logged training entry.
export function countSets(sets: unknown[]) {
  return sets.length
}
